# 🏥 النظام الطبي الذكي – مشروع Node.js

نظام طبي متكامل تم تطويره باستخدام Node.js وExpress، يهدف إلى تسهيل إدارة المرضى، الأطباء، التحاليل، الرسائل، والمواعيد، مع توفير تجربة مستخدم سلسة وحماية أمنية متقدمة.

---

## 📌 نبذة تعريفية

هذا المشروع هو تطبيق ويب طبي يتيح للمستخدمين من مختلف الأدوار (مريض، طبيب، مختبر، مسؤول) التفاعل مع النظام حسب صلاحياتهم.  
تم تصميمه ليكون سهل الاستخدام، قابل للتوسعة، ومؤمن ضد أغلب أنواع الهجمات المنطقية والتقنية.

---

## ⚙️ الوظائف الأساسية

- تسجيل الدخول حسب الدور (مريض، طبيب، مختبر، مسؤول)
- إرسال الرسائل من المرضى إلى الإدارة
- إدارة المواعيد بين المرضى والأطباء
- إضافة نتائج التحاليل من المختبر
- عرض وتحليل بيانات المرضى
- لوحة تحكم للمسؤول لإدارة المستخدمين والأطباء

---

## 🔐 تقرير أمني لموقع النظام الطبي

هذا التقرير يوضح مستوى الأمان العالي الذي يتمتع به الموقع، بناءً على سلسلة من اختبارات الاختراق التي تم تنفيذها يدويًا باستخدام أدوات احترافية مثل Burp Suite وPostman، وباستخدام عقلية المهاجم الذكي.

---

### ✅ أولًا: الاختبارات الأمنية التي تم تنفيذها

| اسم الاختبار              | الوظيفة | شرح مبسط |
|---------------------------|---------|-----------|
| **تقييد الميثودات (HTTP Methods)** | منع استخدام طرق غير مصرح بها مثل DELETE أو PUT | يمنع أي محاولة لإرسال طلب غير متوقع للسيرفر، مثل حذف أو تعديل بدون إذن |
| **تعديل الصلاحيات (Role Injection)** | منع تغيير دور المستخدم إلى مسؤول أو طبيب | يحمي من محاولة التلاعب بالدور داخل الطلبات لتجاوز الصلاحيات |
| **XSS (Cross-Site Scripting)** | منع تنفيذ سكربتات داخل الموقع | يمنع المهاجم من إدخال أكواد خبيثة مثل JavaScript داخل الرسائل أو النماذج |
| **CSRF (Cross-Site Request Forgery)** | منع تنفيذ طلبات من مواقع خارجية باسم المستخدم | يحمي من هجمات تجبر المستخدم على تنفيذ عمليات بدون علمه |
| **Rate Limiting / Spam Abuse** | منع إرسال عدد كبير من الطلبات في وقت قصير | يمنع السبام ويقلل الضغط على السيرفر، ويمنع استغلال النماذج |
| **IDOR (Insecure Direct Object Reference)** | منع الوصول لبيانات مستخدمين آخرين | يمنع المهاجم من تعديل الروابط للوصول لملفات أو بيانات لا تخصه |
| **Mass Assignment** | منع تعديل خصائص غير مصرح بها | يمنع المهاجم من إضافة حقول خفية مثل `isAdmin=true` داخل النموذج |

---

### 🔐 ثانيًا: وسائل الحماية المدمجة داخل الموقع

| نوع الحماية              | الوظيفة | شرح مبسط |
|--------------------------|---------|-----------|
| **تشفير كلمات المرور (bcrypt)** | حماية كلمات المرور من السرقة | حتى لو تم الوصول لقاعدة البيانات، لا يمكن معرفة كلمات المرور لأنها مشفرة |
| **التحقق من عنوان IP** | تحديد هوية الجهاز المرسل | يُستخدم لمنع السبام وتحديد مصدر الطلبات بدقة |
| **التحقق من الجلسة (Session Validation)** | منع الوصول غير المصرح به | كل عملية تتم بناءً على صلاحيات المستخدم المخزنة في الجلسة |
| **تقييد الوصول حسب الدور (Role-Based Access)** | التحكم في من يقدر يشوف أو يعدل البيانات | يمنع المرضى من الوصول لصفحات الأطباء أو المسؤولين والعكس |
| **فلترة الميثودات (restrictMethods)** | منع استخدام طرق HTTP غير مصرح بها | يرفض أي طلب DELETE أو PUT أو PATCH إذا لم يكن مسموحًا به للمسار |
| **التحقق من المصدر (Origin Check)** | حماية من هجمات CSRF | يرفض أي طلب جاي من موقع خارجي غير موثوق |
| **منع السبام (Rate Limiting)** | تقليل الضغط ومنع الإرسال المتكرر | يسمح برسالة واحدة فقط كل 10 دقائق من نفس الجهاز |
| **تعقيم المدخلات (Input Sanitization)** | منع إدخال أكواد خبيثة | يحمي من XSS ويمنع تنفيذ السكربتات داخل الموقع |

---

### 🧨 ثالثًا: تقرير أمان نهائي

بناءً على الاختبارات التي تم تنفيذها يدويًا، وباستخدام أدوات احترافية، فإن الموقع:

- ✅ نجح في صد جميع محاولات الاختراق المنطقي والتقني
- ✅ يحتوي على طبقات متعددة من الحماية تغطي كل الجوانب
- ✅ لا يسمح بتجاوز الصلاحيات أو تنفيذ عمليات غير مصرح بها
- ✅ لا يحتوي على ثغرات XSS أو CSRF أو IDOR أو Mass Assignment
- ✅ يستخدم تشفير قوي لكلمات المرور، ويعتمد على الجلسة للتحقق من الهوية

> 🔐 **الخلاصة**: من الصعب جدًا، بل شبه المستحيل، اختراق هذا الموقع بأي طريقة تقليدية أو منطقية، ما لم يتم الوصول الفعلي إلى السيرفر أو الكود المصدر.  
> الموقع مؤمن بشكل ممتاز، ويتجاوز المعايير الأساسية للأمان في تطبيقات الويب.

---
# 🛡️ تقرير أمني لموقع النظام الطبي

هذا التقرير يوضح مستوى الأمان العالي الذي يتمتع به الموقع، بناءً على سلسلة من اختبارات الاختراق التي تم تنفيذها يدويًا باستخدام أدوات احترافية مثل Burp Suite وPostman، وباستخدام عقلية المهاجم الذكي.

---

## ✅ أولًا: الاختبارات الأمنية التي تم تنفيذها

| اسم الاختبار              | الوظيفة | شرح مبسط |
|---------------------------|---------|-----------|
| **تقييد الميثودات (HTTP Methods)** | منع استخدام طرق غير مصرح بها مثل DELETE أو PUT | يمنع أي محاولة لإرسال طلب غير متوقع للسيرفر، مثل حذف أو تعديل بدون إذن |
| **تعديل الصلاحيات (Role Injection)** | منع تغيير دور المستخدم إلى مسؤول أو طبيب | يحمي من محاولة التلاعب بالدور داخل الطلبات لتجاوز الصلاحيات |
| **XSS (Cross-Site Scripting)** | منع تنفيذ سكربتات داخل الموقع | يمنع المهاجم من إدخال أكواد خبيثة مثل JavaScript داخل الرسائل أو النماذج |
| **CSRF (Cross-Site Request Forgery)** | منع تنفيذ طلبات من مواقع خارجية باسم المستخدم | يحمي من هجمات تجبر المستخدم على تنفيذ عمليات بدون علمه |
| **Rate Limiting / Spam Abuse** | منع إرسال عدد كبير من الطلبات في وقت قصير | يمنع السبام ويقلل الضغط على السيرفر، ويمنع استغلال النماذج |
| **IDOR (Insecure Direct Object Reference)** | منع الوصول لبيانات مستخدمين آخرين | يمنع المهاجم من تعديل الروابط للوصول لملفات أو بيانات لا تخصه |
| **Mass Assignment** | منع تعديل خصائص غير مصرح بها | يمنع المهاجم من إضافة حقول خفية مثل `isAdmin=true` داخل النموذج |

---

## 🔐 ثانيًا: وسائل الحماية المدمجة داخل الموقع

| نوع الحماية              | الوظيفة | شرح مبسط |
|--------------------------|---------|-----------|
| **تشفير كلمات المرور (bcrypt)** | حماية كلمات المرور من السرقة | حتى لو تم الوصول لقاعدة البيانات، لا يمكن معرفة كلمات المرور لأنها مشفرة |
| **التحقق من عنوان IP** | تحديد هوية الجهاز المرسل | يُستخدم لمنع السبام وتحديد مصدر الطلبات بدقة |
| **التحقق من الجلسة (Session Validation)** | منع الوصول غير المصرح به | كل عملية تتم بناءً على صلاحيات المستخدم المخزنة في الجلسة |
| **تقييد الوصول حسب الدور (Role-Based Access)** | التحكم في من يقدر يشوف أو يعدل البيانات | يمنع المرضى من الوصول لصفحات الأطباء أو المسؤولين والعكس |
| **فلترة الميثودات (restrictMethods)** | منع استخدام طرق HTTP غير مصرح بها | يرفض أي طلب DELETE أو PUT أو PATCH إذا لم يكن مسموحًا به للمسار |
| **التحقق من المصدر (Origin Check)** | حماية من هجمات CSRF | يرفض أي طلب جاي من موقع خارجي غير موثوق |
| **منع السبام (Rate Limiting)** | تقليل الضغط ومنع الإرسال المتكرر | يسمح برسالة واحدة فقط كل 10 دقائق من نفس الجهاز |
| **تعقيم المدخلات (Input Sanitization)** | منع إدخال أكواد خبيثة | يحمي من XSS ويمنع تنفيذ السكربتات داخل الموقع |

---

## 🧨 ثالثًا: تقرير أمان نهائي

بناءً على الاختبارات التي تم تنفيذها يدويًا، وباستخدام أدوات احترافية، فإن الموقع:

- ✅ نجح في صد جميع محاولات الاختراق المنطقي والتقني
- ✅ يحتوي على طبقات متعددة من الحماية تغطي كل الجوانب
- ✅ لا يسمح بتجاوز الصلاحيات أو تنفيذ عمليات غير مصرح بها
- ✅ لا يحتوي على ثغرات XSS أو CSRF أو IDOR أو Mass Assignment
- ✅ يستخدم تشفير قوي لكلمات المرور، ويعتمد على الجلسة للتحقق من الهوية

> 🔐 **الخلاصة**: من الصعب جدًا، بل شبه المستحيل، اختراق هذا الموقع بأي طريقة تقليدية أو منطقية، ما لم يتم الوصول الفعلي إلى السيرفر أو الكود المصدر.  
> الموقع مؤمن بشكل ممتاز، ويتجاوز المعايير الأساسية للأمان في تطبيقات الويب.

---