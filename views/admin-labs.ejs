


<%- include('partials/header', { title: 'إدارة المختبرات الطبية' }) %>

    <main class="container">
        <h1>إدارة المختبرات الطبية</h1>
        
        <div class="admin-actions">
            <a href="/dashboard" class="btn">العودة للوحة التحكم</a>
            <a href="/dashboard/add-lab" class="btn">إضافة مختبر جديد</a>
        </div>
        
        <div class="table-responsive">
            <table class="admin-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>الصورة</th>
                        <th>الاسم</th>
                        <th>اسم المستخدم</th>
                        <th>المستشفى</th>
                        <th>البريد الإلكتروني</th>
                        <th>الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    <% labs.forEach(lab => { %>
                        <tr>
                            <td><%= lab.id %></td>
                            <td><img src="<%= 'https://via.placeholder.com/50' %>" alt="<%= lab.name %>" class="doctor-thumb"></td>
                            <td class="editable" data-field="name"><%= lab.name %></td>
                            <td class="editable" data-field="username"><%= lab.username %></td>
                            <td class="editable" data-field="hospital"><%= lab.clinicName %></td>
                            <td class="editable" data-field="email"><%= lab.email %></td>
                            <td class="actions">
                                <button class="btn btn-edit"><a style="text-decoration: none; color: white;" href="/dashboard/edit-lab/<%= lab.id %>">تعديل</a></button>
                                <form method="POST" action="/dashboard/delete-lab/<%= lab.id %>?_method=DELETE" style="display:inline;">
                                    <button class="btn btn-delete"type="submit" onclick="return confirm('هل أنت متأكد من حذف هذا المختبر؟')">🗑️ حذف</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </main>

<%- include('partials/footer') %>